apiVersion: v1
kind: Service
metadata:
  name: locatic-web
  namespace: locatic
  labels:
    app: locatic-web
spec:
  selector:
    app: locatic-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: NodePort